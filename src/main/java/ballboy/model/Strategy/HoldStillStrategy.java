package ballboy.model.Strategy;

import ballboy.model.MovableEntity;

public class HoldStillStrategy implements Strategy{
    @Override
    public void move(MovableEntity movableEntity) {
            // DO NOTHING SINCE HOLD STILL
    }
}
